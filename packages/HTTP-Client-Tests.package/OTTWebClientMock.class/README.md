An OTTWebClientMock is a mocked web client.

Instance Variables
	lastContent:		<String>
	lastRequest:		<WebRequest>
	responseBlock:		<Block>

lastContent
	- content of the last request

lastRequest
	- last request

responseBlock
	- block which returns a response. used when a request is sent to the mock client
